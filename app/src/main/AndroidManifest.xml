<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ro.wethecitizens.firstcontact">

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--Added so we can trigger system alert dialogs, which appear on top of everything the user is doing-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- For Android versions < 24 need to downgrade zxing:core to 3.3.0 or earlier for Android 14+ support. https://github.com/journeyapps/zxing-android-embedded#older-sdk-versions -->
    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <application
        android:name="ro.wethecitizens.firstcontact.TracerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name="ro.wethecitizens.firstcontact.SplashActivity"
            android:configChanges="keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ro.wethecitizens.firstcontact.onboarding.OnBoardingActivity"
            android:screenOrientation="portrait" />
        <!-- this activity will be triggered on infection alert-->
        <activity
            android:name="ro.wethecitizens.firstcontact.AlertActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Dialog.Alert"/>
        <activity
            android:name="ro.wethecitizens.firstcontact.onboarding.HowItWorksActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="ro.wethecitizens.firstcontact.onboarding.PreOnboardingActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="ro.wethecitizens.firstcontact.onboarding.WebViewActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="ro.wethecitizens.firstcontact.MainActivity"
            android:screenOrientation="portrait" />

        <receiver android:name="ro.wethecitizens.firstcontact.boot.StartOnBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>


        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification_service" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_tint" />

        <activity
            android:name="ro.wethecitizens.firstcontact.permissions.RequestFileWritePermission"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.Transparent.NoActionBar" />

        <service
            android:name="ro.wethecitizens.firstcontact.services.BluetoothMonitoringService"
            android:foregroundServiceType="location" />

        <service
            android:name="ro.wethecitizens.firstcontact.services.PeriodicallyDownloadService"
            android:foregroundServiceType="dataSync" />

        <activity
            android:name="ro.wethecitizens.firstcontact.PeekActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.DebugNoActionBar" />

        <activity
            android:name="ro.wethecitizens.firstcontact.PlotActivity"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.DebugNoActionBar" />

        <receiver android:name="ro.wethecitizens.firstcontact.receivers.UpgradeReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />

        <activity
            android:name=".InfectionInstructionsActivity"
            android:screenOrientation="portrait"/>

        <!-- for SMS retriever api -->
        <receiver
            android:name="ro.wethecitizens.firstcontact.fragment.alert.PinSmsBroadcastReceiver"
            android:exported="true"
            android:permission="com.google.android.gms.auth.api.phone.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.gms.auth.api.phone.SMS_RETRIEVED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
